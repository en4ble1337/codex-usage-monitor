# Claude Provider Placeholder

Claude support is intentionally out of scope for the Ida MVP. This folder exists
only to reserve the provider boundary described in `docs/ARCH.md`; no Claude
capture, credentials, fixtures, or product code should be added until a future
directive explicitly owns that work.
